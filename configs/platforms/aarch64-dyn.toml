# Architecture identifier.
arch = "aarch64"                    # str
# Platform identifier.
platform = "aarch64-dyn"      # str

#
# Platform configs
#
[plat]
# Platform family.
family = "aarch64-dyn" # str

# Not used.
kernel-base-vaddr = "0x0"     # uint

# if no will: cannot find value `PHYS_BUS_OFFSET` in module `axconfig::plat`
phys-bus-offset = 0